app-estaciona

Instruções
============
1. Instalar as dependências do node
    ```bash
    npm install
    ```
1. Executar o aplicativo

    ```bash
    # No navegador
    cordova run browser
    
    # No android
    cordova run android
    ```
